# Utilizamos la palabra clave return para que una función devuelva algún valor.
# La instrucción return termina la función
def multiply(a, b=1):
    return a * b


print(multiply(b=4, a=5))  # salida: 20
print(multiply(5))
